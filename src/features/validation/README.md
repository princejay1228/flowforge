# features/validation

UI and orchestration around the deterministic Validation Engine (src/lib/validation). Deep semantic validation (circular dependencies, skill mismatches, impossible schedules, etc.) is NOT implemented yet.

This module is a structural placeholder created during the scaffold phase.
See PROJECT_ROADMAP.md for the full specification and DEVELOPMENT_RULES.md
for rules future coding agents must follow when implementing it.
