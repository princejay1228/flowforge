# features/auth

User authentication and session/account management. FlowForge has no backend, so this is expected to be a lightweight, local-first identity model (or a thin integration with a client-only auth provider) rather than a traditional server-side auth system. NOT IMPLEMENTED.

This module is a structural placeholder created during the scaffold phase.
See PROJECT_ROADMAP.md for the full specification and DEVELOPMENT_RULES.md
for rules future coding agents must follow when implementing it.
